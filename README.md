# sijpkes.github.io

This site is built with Eleventy and Nunjucks.

## Development

Use Node 22 with fnm:

```bash
fnm use 22
npm install
npm start
```

## Build

```bash
fnm use 22
npm run build
```

The generated site will be written to the `_site` folder.
